# Development environment with MOCK mode
# This file enables frontend development WITHOUT running the backend
# All API calls will be intercepted and return mock data

# Enable mock mode (intercepts all /api/* calls)
VITE_MOCK_MODE=true

# API Base URL (not used when mock mode is active)
VITE_API_BASE_URL=http://localhost:7777
