#!/bin/sh
# CloudTouch Pre-Commit Hook
# Runs backend and frontend tests before allowing commit

echo "🔍 Running pre-commit checks..."

# Run backend tests
echo "📦 Backend tests (pytest)..."
cd apps/backend && pytest -v || exit 1
cd ../..

# Run frontend tests
echo "⚛️  Frontend tests (vitest)..."
npm run test:frontend || exit 1

echo "✅ All pre-commit checks passed!"
